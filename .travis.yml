nguage: c
script:
- pwd
- ls
- cd ConsoleTest
- pwd
- g++ -v
- echo "start build program"
- g++ test.cpp -o exe
before_deploy:
- export RELEASE_EXE=$(ls test.exe)
- echo "deploying output"
deploy:
  provider: releases
  file: "${RELEASE_PKG_FILE}" 
  file_glob: true
  skip_cleanup: true
  on: 
     branch: master
